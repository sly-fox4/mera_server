#include "header.h"



int main()
{

	Server s1;
	s1.startListening();
	//StartWinSock();

	//CreateSock();
	//Waiting();
	//closesocket(Listen);
	//closesocket(Connect);
	WSACleanup();
	system("PAUSE");
	return 0;
}
